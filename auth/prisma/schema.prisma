datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model AuthCredential {
  id       String @id @default(uuid())
  userId   String @unique
  email    String @unique
  password String
}
